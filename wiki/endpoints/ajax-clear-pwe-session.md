# AJAX `clear_pwe_session`

Czyści całą bieżącą sesję PHP używaną przez proces rejestracji PWE (`$_SESSION = []`) i zamyka zapis sesji. Handler istnieje zarówno dla użytkowników zalogowanych, jak i `nopriv`.
